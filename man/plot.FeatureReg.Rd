% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-regressor.R
\name{plot.FeatureReg}
\alias{plot.FeatureReg}
\title{Plot a Feature Regressor}
\usage{
\method{plot}{FeatureReg}(
  x,
  grid = NULL,
  show_onsets = FALSE,
  onset_color = "red",
  onset_alpha = 0.5,
  precision = 0.33,
  ...
)
}
\arguments{
\item{x}{A `FeatureReg` object created by [feature_regressor()].}

\item{grid}{Numeric vector of time points for evaluation. If `NULL`
(default), a grid from 0 to max(times) + span with step 0.5s is used.}

\item{show_onsets}{Logical; if `TRUE`, show vertical lines at sample times.
Defaults to `FALSE` because a dense feature has one sample per bin.}

\item{onset_color}{Color for sample-time lines. Default is `"red"`.}

\item{onset_alpha}{Alpha transparency for sample-time lines. Default is 0.5.}

\item{precision}{Numeric sampling precision for HRF evaluation. Default is 0.33.}

\item{...}{Additional arguments passed to the underlying plot functions.}
}
\value{
Invisibly returns a data frame with the time and response values.
}
\description{
Plot a Feature Regressor
}
\examples{
feat <- feature_regressor(abs(sin(seq(0, 8, by = 0.1))), dt = 0.1)
plot(feat, grid = seq(0, 12, by = 0.5))
}
